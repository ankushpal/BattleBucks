package com.example.battlebucks.model

data class RankedPlayer(val rank: Int,
                        val playerId: String,
                        val username: String,
                        val score: Int)
